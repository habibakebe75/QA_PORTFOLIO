# Selenium Java (JUnit5) — Demo Bank

**What it shows**
- Clean JUnit5 test structure
- Selenium 4 with Selenium Manager (no manual driver needed)
- Explicit waits (stability)
- Simple login + assertion on dashboard

**Run**
```bash
cd selenium-java
mvn -q -Dtest=DemoBankLoginTest test
```
